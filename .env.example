# The Flutter client no longer uses an OpenAI key directly — recipe import
# and macro estimation now run through Cloud Functions (see functions/.env.example).
# This file is intentionally empty; nothing here is bundled into the app.
